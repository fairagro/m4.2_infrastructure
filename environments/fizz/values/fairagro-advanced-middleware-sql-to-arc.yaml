# fizz — scheduled converter CronJob (periodic Edaphobase → ARC runs)
image:
  tag: 1.2.1-fixes-from-prod.1

converter:
  cronjob:
    enabled: true

config:
  log_level: INFO
  rdi: edaphobase
  rdi_url: https://edaphobase.org
  max_concurrent_arc_builds: 4
  api_client:
    api_url: "https://middleware-test.fairagro.net"
    timeout: 600
    client_cert_path: /run/secrets/tls/client.crt
    client_key_path: /run/secrets/tls/client.key
    verify_ssl: true
